name: Release Wallet Extension

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      # Chrome 构建与上传
      - name: Build Chrome Extension
        run: npm run build:chrome

      - name: Upload Chrome zip to Release
        uses: softprops/action-gh-release@v1
        with:
          files: ksd-wallet-chrome.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Edge 构建与上传
      - name: Build Edge Extension
        run: npm run build:edge

      - name: Upload Edge zip to Release
        uses: softprops/action-gh-release@v1
        with:
          files: ksd-wallet-edge.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Firefox 构建与上传
      - name: Build Firefox Extension
        run: npm run build:firefox

      - name: Upload Firefox zip to Release
        uses: softprops/action-gh-release@v1
        with:
          files: ksd-wallet-firefox.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
